- name: Setup wacli notifier
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    ansible_user: reed
    home_dir: /home/{{ ansible_user }}
    wacli_dir: "{{ home_dir }}/proj/wacli"

  tasks:
    - name: Create systemd user directory
      ansible.builtin.file:
        path: "{{ home_dir }}/.config/systemd/user"
        state: directory
        mode: "0755"

    - name: Install wacli-notifier service
      ansible.builtin.template:
        src: wacli-notifier.service.j2
        dest: "{{ home_dir }}/.config/systemd/user/wacli-notifier.service"
        mode: "0644"

    - name: Reload systemd user daemon
      ansible.builtin.systemd:
        daemon_reload: true
        scope: user

    - name: Enable and start wacli-notifier
      ansible.builtin.systemd:
        name: wacli-notifier
        enabled: true
        state: started
        scope: user
